---
description: RHF Controller + MUI TextField component.
---

A wrapper around RHF Controller and MUI TextField with an optional label

### Component usage

```js
const { control } = useFormContext();

<TextInput
  control={control}
  name="name"
  label="Name"
  textFieldProps={{
    placeholder: 'Insert your name',
  }}
/>;
```
