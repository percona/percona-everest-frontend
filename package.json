{
  "name": "percona-ui-monorepo",
  "version": "0.0.1",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.10.16"
  },
  "scripts": {
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "build": "turbo run build && turbo run move-build"
  }
}
