import React from 'react';
import { OverviewCardProps } from './overview-card.types';
import { Box } from '@mui/material';

export const OverviewCard = ({ title }: OverviewCardProps) => (
  <Box>
    
  </Box>
)
