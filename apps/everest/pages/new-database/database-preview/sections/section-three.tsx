import React from "react";
import { PreviewContentText } from "../preview-section";
import { SectionProps } from "./section.types";

export const PreviewSectionThree = ({ }: SectionProps) => (
  <PreviewContentText text='Daily at 1 AM' />
)
