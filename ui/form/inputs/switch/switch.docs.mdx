---
description: RHF Controller + MUI Switch component.
---

A wrapper around RHF Controller and MUI Switch

### Component usage
```js
  const { control } = useFormContext();

  <SwitchInput
    control={control}
    label='Backups'
    name='backupsEnabled'
  />
```
