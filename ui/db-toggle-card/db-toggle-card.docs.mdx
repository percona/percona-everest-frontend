---
description: A ToggleCard meant to pick a DB type within a ToggleButtonGroup.
---

This component is a composition of `ToggleCard`. By passing `value` as a `DbType`, a DB icon and corresponding text will be renderd in the card.

### Component usage

```js
<DbToggleCard value={DbType.Postresql} />
```
