---
description: Database icons.
---

import { MySqlIcon, MongoIcon, PostgreSqlIcon } from './db';

A component that does something special and renders text in a div.

### Component usage

```js
<MySqlIcon />
<MongoIcon />
<PostgreSqlIcon />
```
